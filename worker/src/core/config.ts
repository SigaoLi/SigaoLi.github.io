// 费用护栏(PRD §22.4「费用四件套」的请求侧三件;第四件 = 各提供商后台的额度硬顶)。

export const LIMITS = {
  /** 单会话轮数上限(按 user 消息数计,超出要求访客刷新重开) */
  maxTurns: 12,
  /** 单条用户消息长度上限(字符) */
  maxUserChars: 2000,
  /** 传给模型的历史总长上限(字符,超出从最旧开始丢弃) */
  maxHistoryChars: 16000,
  /**
   * 单次回复 max_tokens。**推理 token 与可见回答共享这一个预算**——DeepSeek 没有
   * max_reasoning_tokens / budget_tokens 之类的上限参数,压不住推理用量(reasoning_effort 也只有
   * low/high/max 三档,且 v4-pro 的 low 会被内部映射回 high)。
   * 2026-08-05 实测:问「把每一段经历都详细讲一遍」时推理独吞 1129/1200,访客只看到 109 字就被截断,
   * 且问题越宽泛推理越多、留给回答的越少——最需要篇幅的问题可用篇幅最小。故抬到 2400 留出余量。
   * 这只是天花板,按实际用量计费:典型问答仍只花 160-380 token(≈130-540 字),日常成本不变。
   * 真正防"答得太长"的是 prompt 里的篇幅规范(见 prompt.ts「回答长度」),不是这个数。
   * 下限注意:推理模型须 ≥1000,否则推理吃光预算出现空回复(更早的实测坑)。
   */
  maxTokens: 2400,
  /** 上游建立连接+返回响应头的超时;超时视同该提供商失败,回退链切下一家(拿到流后即解除,不限制流本身时长) */
  upstreamConnectMs: 30_000,
};

/** 知识包 isolate 内存缓存时长 */
export const KNOWLEDGE_TTL_MS = 10 * 60 * 1000;
